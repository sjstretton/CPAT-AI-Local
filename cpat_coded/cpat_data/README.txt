This folder contains Egypt (EGY), Middle East & North Africa (MEA) and global data files required for `cpat_sisepuede` repository.

`new_data` and `new_data_pkl` folders contain `.csv` and `.pkl.bz2` data files accordingly. They should be placed in folders with the same names that located in `cpat_data/` dir.
